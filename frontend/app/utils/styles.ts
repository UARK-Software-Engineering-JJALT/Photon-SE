const bigTimer = {
    fontFamily: "Arial, sans-serif",
    fontSize: "48px",
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    height: "100vh",
    color: "#ff580f"
};

const smallTimer = {
    fontFamily: "Arial, sans-serif",
    fontSize: "32px",
    color: "#ff580f",
};

export { bigTimer, smallTimer };